mutation updateBookshelf($name: String!, $id: Float!) {
  updateBookshelf(id: $id, options: { name: $name }) {
    errors {
      field
      message
    }
    bookshelf {
      id
      name
    }
  }
}
